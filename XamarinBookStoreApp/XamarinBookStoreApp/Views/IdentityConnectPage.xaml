<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinBookStoreApp.Views.IdentityConnectPage"
              Title="{Binding Title}">
    <ContentPage.Content>
        <StackLayout Padding="10,0,10,0" VerticalOptions="Center">
            <Button VerticalOptions="Center" Text="Login" Command="{Binding LoginIdentityServerCommand}"/>
            <Button VerticalOptions="Center" Text="Logout" Command="{Binding LogoutIdentityServerCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>